{
  "name": "ploom-tests",
  "version": "1.0.0",
  "description": "Playwright test suite for Ploom",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright test --reporter=html",
    "prepare": "playwright install"
  },
  "keywords": ["playwright", "testing"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "playwright": "^1.50.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}